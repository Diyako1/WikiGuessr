# Database connection string (optional - app works without it)
DATABASE_URL="postgresql://username:password@localhost:5432/wikiguessr?schema=public"

# Redis connection URL (optional - falls back to in-memory cache)
REDIS_URL="redis://localhost:6379"
